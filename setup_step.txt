pip install dualsense-controller
sudo apt-get install ros-humble-imu-tools
sudo apt install libhidapi-dev


# or create a file locally /etc/udev/rules.d/70-dualsense.rules with following content.

# USB
KERNEL=="hidraw*", SUBSYSTEM=="hidraw", ATTRS{idVendor}=="054c", ATTRS{idProduct}=="0ce6", MODE="0666"
# Bluetooth
KERNEL=="hidraw*", SUBSYSTEM=="hidraw", KERNELS=="0005:054C:0CE6.*", MODE="0666"


# setup ssh server for zed editor
# inside docker container
sudo apt-get install -y openssh-server
sudo service ssh start
# change password
sudo passwd ${USER}
sudo service ssh restart

# connect from local terminal once then connect using zed
ssh zhekai@localhost
